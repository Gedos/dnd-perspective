import {library} from '@fortawesome/fontawesome-svg-core';
import {faCaretUp, faCaretLeft, faCaretRight, faCaretDown, faCircle} from '@fortawesome/free-solid-svg-icons';

export default () => library.add(
  faCaretUp,
  faCaretLeft,
  faCaretRight,
  faCaretDown,
  faCircle
);

